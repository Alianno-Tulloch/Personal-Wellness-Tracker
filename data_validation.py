from datetime import datetime


def validate_date(date_str):
    # Note: Date will be given as scroll wheel input in the GUI, so less likely to be invalid
    if not date_str

def validate_hours_slept(hours_slept):
    # Note - the textbox will only accept number values enterred, and it won't register non-numeric input
    # Note 2 - change to some type of datetime

def validate_exercise_minutes(exercise_minutes):
    # Note - the textboxes will only accept number values enterred, and it won't register non-numeric input
    # Note 2 - change to some type of datetime

def validate_mood_scale(mood_scale):
    # Note - the textbox will only accept number values enterred, and it won't register non-numeric input

def validate_mood_description(mood_description):
    # just a check to make sure that the user entered something... can be moved to GUI

def validate_activities(activities):
    # just a check to make sure that the user entered something... can be moved to GUI

# Note: Might remove, because notes isnt a required field

def validate_notes(notes):
    # optional field, so no validation needed